# API
CORS_ALLOW_ORIGINS=http://localhost:3000

# Storage
AZURE_STORAGE_CONN=DefaultEndpointsProtocol=...;AccountName=...;AccountKey=...;EndpointSuffix=core.windows.net
BLOB_CONTAINER=reports

# Output folder (local staging)
BASE_OUT=generate_reports
TZ_NAME=Asia/Kolkata

# Logging DB (where we store job + artifacts)
PG_DSN=host=... dbname=... user=... password=... port=5432 sslmode=require

# Source DB (where PO data lives) — only needed if you fetch inside API
PG_DSN_SOURCE=host=... dbname=... user=... password=... port=5432 sslmode=require
